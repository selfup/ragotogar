[project]
name = "ragotogar-tools"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "lightrag-hku",
    "httpx",
    "numpy",
    "openai",
    "pytest",
]

[tool.pytest.ini_options]
testpaths = ["."]
python_files = ["*_test.py"]
