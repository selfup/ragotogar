[project]
name = "ragotogar-tools"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "asyncpg>=0.31.0",
    "httpx",
    "numpy",
    "openai",
    "pgvector>=0.4.2",
    "pytest",
]

[tool.pytest.ini_options]
testpaths = ["."]
python_files = ["*_test.py"]
