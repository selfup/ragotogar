# compiled Go binaries (never commit — always use go run)
cmd/describe/describe
cmd/organize/organize
cmd/cashier/cashier
cmd/web/web
cmd/index/index
cmd/search/search
.test_log.txt
.env
describe_test*
describe_*
# Legacy local-only state from the SQLite + LightRAG era. Postgres replaces
# both; these dirs aren't read by anything anymore but may still exist on
# machines that ran the old pipeline. Safe to `rm -rf` whenever.
tools/.rag_index
tools/.sql_index
.DS_Store
._*
.claude/*
!.claude/settings.local.json
descriptions
mistralai
001-*
full_run.log
**.env
